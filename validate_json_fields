
def validate_required_fields(response_json,required_fileds):
    keys = required_fileds.split(".")
    current =  response_json
    for key in keys:
       if isinstance(current,dict):
           if key not in current:
               return False
           current = current[key]
    return True

response_json = {
  "id": 100,
  "user": {
    "name": "Rama",
    "age": 33,
    "Address": {
      "city": "hyd"
    }  
     }
}

print(validate_required_fields(response_json,"id"))  #True
print(validate_required_fields(response_json, "user.name")) #True
print(validate_required_fields(response_json, "user.address.zipcode")) #False
}

